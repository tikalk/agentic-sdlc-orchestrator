{{- if .Values.configMap.create }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "agentic-sdlc-agent-runner.configMapName" . }}
  namespace: {{ include "agentic-sdlc-agent-runner.namespace" . }}
  labels:
    {{- include "agentic-sdlc-agent-runner.labels" . | nindent 4 }}
data:
  # Server password is now sourced from ExternalSecret, not ConfigMap
  # See external-secret.yaml for secret management
  {{- with .Values.configMap.data }}
  {{- toYaml . | nindent 2 }}
  {{- end }}
{{- end }}
